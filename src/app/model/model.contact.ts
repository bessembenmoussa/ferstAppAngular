import {Category} from './model.category';

export class Contact {
  id: null;
  nom: string;
  prenom: string;
  dateNaissance: Date;
  email: string;
  tel: number;
  photo: '';
  category: Category ;
}
